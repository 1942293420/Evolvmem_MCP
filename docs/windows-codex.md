# Windows 原生 Codex：EvolvMem 完整功能对齐计划

状态：用户已批准并实施；服务端回归与最终代码复核通过，交付 Windows 验收版本。Windows 原生桌面实机验收由用户运行安装和验收命令。

## 目标、验收与边界

- 一次安装、一个 EvolvMem MCP 配置入口，使 Windows 原生 Codex 与 Linux 共用同一用户的完整记忆能力。
- 验收覆盖身份、上下文注入、会话归档与总结、项目、任务续接、经验反馈、看板和维护；以同一用户在 Linux 的现有能力为对照。
- 存储沿用原文加密文件与 SQLite 索引、记忆、摘要、任务断点；复用现有提炼器与向量服务。
- 默认覆盖安装后新建和继续使用的本地会话；旧历史批量回填不在本轮范围。
- 仅归档 Codex 已持久化的转写；未落盘、已被删除且未采集的数据不能承诺恢复。
- 可信内网、低并发、现有服务进程；不增加登录系统、数据库服务、Windows 向量模型或 SQLite 文件同步。
- 安装与验收命令见 README 的 Windows 专节；完整功能验收需通过原生桌面版实测，Linux 测试不能替代。

## 一、交付结构

Windows 接入包 → 一个远程 EvolvMem MCP → 现有 Linux 记忆核心。

- 接入包包含用户级安装器、PowerShell 接入脚本、全局 Codex hooks 和本地待上传队列。
- 同一个远程 MCP 入口供 Codex 调工具及本地脚本调用；脚本负责会话事件、转写和本机 Git 观察。
- Windows 任务计划执行同一脚本的采集与上传模式，支持应用关闭后的补传；不再增加 Windows 服务。
- Linux 继续负责身份路由、SQLite、加密归档、提炼、项目、续接、经验与维护。
- 安装器合并现有配置并备份；升级不重复注册，卸载只移除自身配置和任务。
- 单填 MCP URL 不具备宿主事件与本地文件采集能力，无法独立完成全套自动化。

## 二、能力与实现方式

| 能力 | 设计 | 验收结果 |
| --- | --- | --- |
| 身份与跨端共用 | 沿用个人凭据，服务端返回 authenticated_user；客户端核对 expected_user | 两端读写同一用户库；凭据不符停止记忆读写并提示 |
| 自动注入 | 启动、恢复、清空、压缩后由命令 hook 拉取并交付上下文；提示词前检查缺失 | 新会话无需手动要求回忆即可取得既有摘要 |
| 会话归档 | 采集已落盘转写，按会话与版本上传并加密，SQLite 仅保存索引 | 可查归档版本、覆盖范围与处理状态 |
| 总结与记忆 | 复用现有提炼、候选筛选、会话摘要与项目滚动摘要 | Windows 新内容可在新会话和 Linux 查询 |
| 项目管理 | 稳定项目标识、中文别名、显式工作区映射 | 不同系统路径归同一项目，同名目录不误合并 |
| 任务续接 | 复用 begin、checkpoint、resume、find、bind 与版本比较 | 跨端恢复目标、决策、进度和下一步；代码差异明确提示 |
| 经验生命周期 | 召回、候选、确认、实际使用和结果反馈共用现有逻辑 | 成功、失败、不适用等反馈可追溯到真实事件 |
| 记忆管理与维护 | 对齐查询、增改删、确认、整理、项目归档、清理与健康状态 | 同一用户的 Linux 功能均有对应入口和测试 |
| 项目看板 | 复用现有同步与状态查询，保持现有配置和调用授权 | 能查询状态，并在已授权调用下同步真实断点 |
| 故障与恢复 | 待上传数据本地持久化，稳定请求标识，分阶段回执 | 断网不假报已保存，恢复后补传且不重复覆盖新内容 |

身份依据是 EvolvMem 凭据所映射的用户，不是 Windows 用户名或 Codex 登录账号；device_id 只区分设备。
跨端共用同一服务端数据。提交后另一端下次读取可见；已打开的会话在下一次提示词或恢复时检查版本。
记忆共用不包含源代码文件同步；Git 分支、提交与工作区变化均从当前机器实际读取。

## 三、上下文注入流程

1. 安装时核对桌面版版本、全局 hooks 是否可用、凭据和项目绑定，实际触发一次加载。
2. SessionStart 覆盖 startup、resume、clear、compact；命令脚本独立访问 MCP，避开客户端连接尚未准备好的时序。
3. 加载通用记忆、项目滚动摘要、相关会话摘要与已绑定任务断点，默认总预算 8000 字符。
4. 通过 hookSpecificOutput.additionalContext 交付给 Codex；配置输出阈值，避免有界摘要又被宿主缩成预览。
5. UserPromptSubmit 检查本次启动回执，补取失败或遗漏的加载，并按当前问题补充相关经验、处理服务端版本变化。
6. 同次启动去重；恢复和压缩视为新的加载机会。远程初始化说明避免模型重复执行已由 hook 完成的启动加载。
7. 回执包含身份、项目、摘要与断点版本、获取状态；通过宿主事件或转写核验实际交付，不能把仅成功联网写成已注入。
8. 空库允许有效空结果。服务不可用时提示后继续，不擅自启用严格阻断，也不把旧回执当本次成功。

历史摘要作为参考内容封装，不能覆盖当前用户指令；未绑定目录仅加载通用记忆并标记待归属。

## 四、采集、归档与提炼流程

- 使用 session_id、transcript_path 和 cwd 登记会话；保留事件标识及工具、用户消息，不以助手自述替代证据。
- Stop、PreCompact 和短时结束事件触发增量采集；后台任务定期扫描已登记会话，弥补异常退出未触发结束事件。
- SessionEnd 与 Interrupt 最长只有 3 秒，仅读取未采集尾部并落本地队列，不等待网络或模型；超时覆盖不足必须标记。
- 子代理有独立转写时保留与主会话的关联并去重；主会话中已有的工具结果保留来源。
- 待上传正文用 Windows 当前用户的系统加密能力保护；服务端暂存和正式文件沿用现有归档加密。
- 分块最多 256 KiB 原始字节，校验内容摘要、总长度和偏移；只提交完整记录，残缺末行等待后续采集。
- 以认证用户、设备、会话和内容版本去重；追加须核对旧前缀，旧版本不能覆盖新版本，分叉不能仅按长度覆盖。
- 归档完成后复用现有提炼器；短时连续变化合并提炼最新版本，同一版本的重复触发不重复提交记忆。
- 输出会话总结并更新项目滚动摘要；未验证经验仍为候选，后台总结不得直接宣布任务完成。
- 未绑定或混合项目会话保持待归属，不能猜入某个项目；明确后再生成对应项目记录。
- 分别显示待上传、已归档、待提炼、已提炼和失败原因；提炼失败不伪装成全流程成功。
- 沿用现有归档期限及证据保留规则；仅在服务端确认完整归档后清理对应本地待传版本。

## 五、补齐现有远程缺口

- 已接入原先远程禁用的 memory_consolidate、context_sweep、context_archive_project、project_board_sync、project_board_status。
- 已接入远程 context_record_outcome 和带 evidence 的 experience_record；归档事件验证沿用核心的证据要求。
- 增加归档事件解析：认证用户 → 归档版本 → 会话与事件 → 引用内容；服务端不打开 Windows 路径。
- 归档未到达时反馈保持待验证；到达后调用相同验证逻辑，保留“客户端上报”来源性质。
- 维护和看板使用同一用户对应的既有权限、配置与服务端数据范围，分别适配，不能只删除禁用判断。
- 跨设备目录须明确绑定并核对实际 Git 信息，保留现有焦点规则；缺失观察报告未知。
- 里程碑与已确认决定按 Linux 约定立即写断点；后台补录仅处理明确绑定、来源可核对的缺失进展。
- 断点更新沿用 revision 比较；离线排队的旧更新出现冲突时重新读取，不能覆盖较新的跨端进度。
- 清单记录每个工具与自动流程的对齐结果；本用户仍有应有能力缺失时不得宣称完整交付。

## 六、实施顺序与每步可见结果

1. 接通身份与启动注入：核对 Linux 工具清单，实现安装、自检和真实 hook 加载；新 Windows 会话显示正确用户与项目摘要。
2. 跑通会话闭环：实现采集、上传、加密归档和提炼；Windows 会话中的测试事实可在新会话及 Linux 被读回。
3. 完成项目与续接：接入目录映射、别名、滚动摘要、断点和 Git 核验；两端接续同一任务且两个项目互不混入。
4. 对齐剩余能力：完成经验事件验证、结果反馈、整理维护和看板；逐项能力清单通过并可查真实处理结果。
5. 实机验收与发布：验证安装升级、中文及空格路径、压缩、断网补传、异常退出、多会话和双端写入；更新现有图文并提交发布。

每阶段只补验证该阶段所需的测试；确认失败样例后实现、运行相关回归并展示结果，不预建独立基础设施。

## 七、预计改动位置

- scripts/windows/install-evolvmem.ps1：用户级安装、配置合并、自检、升级和卸载。
- scripts/windows/evolvmem-codex.ps1：注入、项目映射、Git 观察、采集、加密队列、上传与状态。
- evolvmem/lan_tools.py、lan_runtime.py、lan_context.py：身份与能力回执、远端路由、工作区适配及处理调度。
- 新增 evolvmem/lan_capture.py、codex_transcript.py：分块接收、版本状态、转写解析与归属。
- evolvmem/session_archive.py、experience_sources.py：复用归档，支持按归档版本定位远端事件。
- evolvmem/kimi_hooks.py、context_service.py：按需提取共享提炼入口，保持既有 Linux 语义。
- 对应 LAN、Windows、归档、经验、续接测试；README 和现有架构图册按最终实现更新。

## 八、最终验收场景

1. 同一身份的 Windows 新会话自动带入 Linux 项目摘要；错配身份不读写错误用户库。
2. Windows 真实会话被加密归档并总结；新窗口和 Linux 能读取新记忆及下一步。
3. 恢复、清空、手动和自动压缩后重新加载；空库、超时、钩子未启用可明确区分。
4. 两个项目、多会话及不同系统路径正确归属；未绑定目录不猜项目或抢占焦点。
5. 真实工具验证可形成经验反馈；助手自述不通过，重复事件不重复计数，归档更新不破坏旧引用。
6. 断网、异常退出、重启后已持久化队列可补传；旧归档、旧断点和重复请求不覆盖新结果。
7. Linux 回归通过；本用户维护、看板与全部工具逐项通过，实际 Windows 结果单独记录。

## 已知状态与依据

实现包含客户端安装、自检、注入、采集与后台上传；服务端加密归档、提炼、摘要、远程经验、维护、看板和保守断点补录。
完整回归已有 2203 项通过、6 项跳过；跳过项为三个既有模型可选测试和 Windows PowerShell 5.1 编码、两项 DPAPI 实测。
最终安装兼容性修正后的 Windows 全套为 35 项通过、3 项跳过；独立限定复核无剩余阻断项。
隔离的合成会话通过现有真实模型完成归档、总结和决定读回；Windows 桌面版本、事件交付与计划任务仍待实测，最终发布与追加验证以交付回执为准。
官方依据：[Codex Hooks](https://learn.chatgpt.com/docs/hooks)、[Codex MCP](https://learn.chatgpt.com/docs/extend/mcp?surface=cli)。
官方明确转写格式不是稳定接口，因此记录实际支持的桌面版范围，升级后用代表性会话复验。
